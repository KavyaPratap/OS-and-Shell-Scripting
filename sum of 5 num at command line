
#wap to find the average of number given in command line

sum=0
count=$#
echo "$count"
for i in "$@"; do
sum=$((sum+i))
echo "sum : $sum"
done
avg=$((sum/count))
echo "average: $avg"
#taking output run as ./loop.sh 1 2 3 4 .........1 2 3 4 are numbers and loop.sh is filename
